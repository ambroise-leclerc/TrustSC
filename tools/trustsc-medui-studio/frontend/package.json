{
  "name": "trustsc-medui-studio-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only MedUI Studio previewer (ADR-022 wave S9). Plain TypeScript, no framework, no bundler.",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/styles.css dist/styles.css && cp src/index.html dist/index.html"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
