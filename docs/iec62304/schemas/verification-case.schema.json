{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mdux.dev/schemas/iec62304/verification-case.schema.json",
  "title": "IEC 62304 Verification Case",
  "description": "Field-aligned with crates/mdux-governance/src/lib.rs::VerificationCase. Relevant to IEC 62304 §5.5-§5.8 (unit/integration/system verification) and §6.2 (problem/modification analysis) — see docs/iec62304/04-development-implementation-and-testing.md.",
  "version": "1.0.0",
  "type": "object",
  "required": ["id", "requirement", "method", "evidence"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Non-empty free-text identifier, e.g. 'VER-EEG-ALERT-LATENCY-01'."
    },
    "requirement": {
      "type": "string",
      "description": "The Requirement id this case verifies. mdux_governance::ComplianceProgram::validate() rejects a VerificationCase whose requirement does not reference a known Requirement (an 'orphan verification')."
    },
    "method": {
      "type": "string",
      "enum": ["analysis", "inspection", "test", "demonstration"],
      "description": "Corresponds to mdux_governance::VerificationMethod's four variants."
    },
    "evidence": {
      "type": "string",
      "description": "Where the verification evidence lives — a test name, a --verify-ui report path, a baker report.json, etc."
    }
  },
  "additionalProperties": false
}
