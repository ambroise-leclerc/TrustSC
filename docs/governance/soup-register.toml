schema_version = 1
register_kind = "trustsc-soup-register"
scope = "host-tooling-and-presentation-adapters"
owner = "TrustSC text authoring maintainers"
yocto_policy = "Future Yocto integration consumes approved font assets, notices, manifest metadata, and generated package outputs; it does not install host baker/shader-baker tooling or their third-party crates into the target image. Presentation-adapter dependencies (ash, ash-window, raw-window-handle, winit) are Class B host-bring-up dependencies reviewed per ADR-012; see each entry's runtime_deployment and integration_path fields."

[[entries]]
component_id = "fontdue-0.9.3"
name = "fontdue"
version = "0.9.3"
classification = "SOUP"
supplier = "crates.io / fontdue maintainers"
repository = "https://github.com/mooman219/fontdue"
license = "MIT OR Apache-2.0 OR Zlib"
usage = "Loads vendored TTF bytes, computes glyph metrics, and rasterizes glyph bitmaps during host-only Roboto atlas compilation."
integration_path = "tools/trustsc-font-baker"
pinned_by = ["tools/trustsc-font-baker/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Used only while baking approved text packages on engineering hosts. Device/runtime artifacts consume only reviewed generated package bytes and digests."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
    "Validate the vendored Roboto digest and byte length against assets/fonts/roboto/font-manifest.toml before rasterization.",
    "Require deterministic rebuild verification against generated/fonts/roboto-regular-16px outputs.",
]

[[entries]]
component_id = "serde-1.0.228"
name = "serde"
version = "1.0.228"
classification = "SOUP"
supplier = "crates.io / serde-rs maintainers"
repository = "https://github.com/serde-rs/serde"
license = "MIT OR Apache-2.0"
usage = "Deserializes host-only bake recipes and generated package.json inputs, and serializes reviewed bake outputs. Also deserializes authored scenario TOML (ADR-016 §4) in crates/trustsc-build, which compiles them to static Rust data consumed at runtime. Also deserializes ML bake recipes and evidence emission in tools/trustsc-ml-baker (ADR-017), and shader-bake recipes/reports in tools/trustsc-shader-baker (ADR-012). Also derives Serialize/Deserialize for the MedUI Studio server's `/api/screens` JSON DTO in tools/trustsc-medui-studio (ADR-022) — the DTO is a tool-zone type, never a governed-crate one."
integration_path = "tools/trustsc-font-baker, crates/trustsc/build.rs, crates/trustsc-build (scenario compilation), tools/trustsc-ml-baker, tools/trustsc-shader-baker, tools/trustsc-medui-studio"
pinned_by = ["tools/trustsc-font-baker/Cargo.toml", "crates/trustsc/Cargo.toml", "crates/trustsc-build/Cargo.toml", "tools/trustsc-ml-baker/Cargo.toml", "tools/trustsc-shader-baker/Cargo.toml", "tools/trustsc-medui-studio/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Confined to host authoring input/output handling; no serde API crosses into the runtime package contract."
risk_controls = [
    "Use deny_unknown_fields on bake-side schemas to prevent silent config drift.",
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
]

[[entries]]
component_id = "serde-json-1.0.150"
name = "serde_json"
version = "1.0.150"
classification = "SOUP"
supplier = "crates.io / serde-rs maintainers"
repository = "https://github.com/serde-rs/json"
license = "MIT OR Apache-2.0"
usage = "Emits deterministic host-side package.json and report.json artifacts for review and reproducibility checks, and parses approved package.json inputs into generated Rust source during trustsc builds. Also parses the safetensors container header in crates/trustsc-ml-authoring (ADR-017 §6, avoiding a dedicated safetensors crate dependency), and parses/emits ML model package.json/report.json evidence in tools/trustsc-ml-baker and crates/trustsc-build model codegen, and shader package.json/report.json evidence in tools/trustsc-shader-baker (ADR-012). Also serializes the MedUI Studio server's `/api/screens` JSON response body in tools/trustsc-medui-studio (ADR-022)."
integration_path = "tools/trustsc-font-baker, crates/trustsc/build.rs, crates/trustsc-ml-authoring, tools/trustsc-ml-baker, crates/trustsc-build (model codegen), tools/trustsc-shader-baker, tools/trustsc-medui-studio"
pinned_by = ["tools/trustsc-font-baker/Cargo.toml", "crates/trustsc/Cargo.toml", "crates/trustsc-ml-authoring/Cargo.toml", "tools/trustsc-ml-baker/Cargo.toml", "crates/trustsc-build/Cargo.toml", "tools/trustsc-shader-baker/Cargo.toml", "tools/trustsc-medui-studio/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Only the generated JSON artifacts cross the boundary; the crate itself remains a host build dependency."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
    "Use byte-for-byte verification to confirm generated JSON artifacts match a fresh rebuild.",
]

[[entries]]
component_id = "sha2-0.10.9"
name = "sha2"
version = "0.10.9"
classification = "SOUP"
supplier = "crates.io / RustCrypto maintainers"
repository = "https://github.com/RustCrypto/hashes"
license = "MIT OR Apache-2.0"
usage = "Computes manifest, atlas, package, and report digests during deterministic host-side baking and verification. Also computes weights/package/report digests in crates/trustsc-ml-authoring and tools/trustsc-ml-baker (ADR-017), and shader package/report digests in tools/trustsc-shader-baker (ADR-012)."
integration_path = "tools/trustsc-font-baker, crates/trustsc-text-authoring, crates/trustsc-ml-authoring, tools/trustsc-ml-baker, tools/trustsc-shader-baker"
pinned_by = ["tools/trustsc-font-baker/Cargo.toml", "crates/trustsc-text-authoring/Cargo.toml", "crates/trustsc-ml-authoring/Cargo.toml", "tools/trustsc-ml-baker/Cargo.toml", "tools/trustsc-shader-baker/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Digest values are consumed as evidence, but the hashing crate stays in host-side tooling and authoring flows."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
    "Record resulting SHA-256 values in source manifests and generated reports for independent verification.",
]

[[entries]]
component_id = "toml-1.1.2+spec-1.1.0"
name = "toml"
version = "1.1.2+spec-1.1.0"
classification = "SOUP"
supplier = "crates.io / toml-rs maintainers"
repository = "https://github.com/toml-rs/toml"
license = "MIT OR Apache-2.0"
usage = "Parses font manifests and bake recipes that define approved Roboto source inputs and Yocto handoff metadata. Also parses authored scenario scripts (examples/<app>/verify/scenarios/*.toml, ADR-016 §4) in crates/trustsc-build, compiling them into static ScenarioScript data at build time — no TOML parsing at runtime. Also parses host-only ML bake recipes in tools/trustsc-ml-baker (ADR-017) and shader-bake recipes in tools/trustsc-shader-baker (ADR-012)."
integration_path = "tools/trustsc-font-baker, crates/trustsc-build (scenario compilation), tools/trustsc-ml-baker, tools/trustsc-shader-baker"
pinned_by = ["tools/trustsc-font-baker/Cargo.toml", "crates/trustsc-build/Cargo.toml", "tools/trustsc-ml-baker/Cargo.toml", "tools/trustsc-shader-baker/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "TOML parsing is confined to host-side authoring configuration (font manifests/recipes, and now scenario scripts); target systems consume the already-reviewed generated outputs instead."
risk_controls = [
    "Fail parsing on invalid manifests or recipes rather than recovering silently.",
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
]

[[entries]]
component_id = "ash-0.38.0+1.3.281"
name = "ash"
version = "0.38.0+1.3.281"
classification = "SOUP"
supplier = "crates.io / ash maintainers"
repository = "https://github.com/ash-rs/ash"
license = "MIT OR Apache-2.0"
usage = "Vulkan 1.x bindings for instance/device/swapchain setup, pipeline creation, and frame submission, confined to the trustsc-vulkan-winit presentation adapter (ADR-012) and used transitively by every example that depends on it (hello_world, class_c_monitor), and by tools/trustsc-medui-studio's offscreen render bridge (ADR-022 wave S7 — headless instance/device only, no window or swapchain)."
integration_path = "adapters/trustsc-vulkan-winit"
pinned_by = ["adapters/trustsc-vulkan-winit/Cargo.toml", "Cargo.lock"]
runtime_deployment = true
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Confined to the trustsc-vulkan-winit edge adapter (ADR-012). No ash::vk type crosses into a governed crate's public API."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
    "The adapter only accepts owned trustsc types (Framework, CompiledScreenPackage, ScreenTextLayout) at its public boundary.",
]

[[entries]]
component_id = "ash-window-0.13.0"
name = "ash-window"
version = "0.13.0"
classification = "SOUP"
supplier = "crates.io / ash maintainers"
repository = "https://github.com/ash-rs/ash"
license = "MIT OR Apache-2.0"
usage = "Creates a Vulkan surface from a winit window handle, confined to the trustsc-vulkan-winit presentation adapter (ADR-012). Compiled into (but never called by) tools/trustsc-medui-studio too, since it depends on the same adapter crate for its offscreen-only render bridge (ADR-022 wave S7), which never creates a surface."
integration_path = "adapters/trustsc-vulkan-winit"
pinned_by = ["adapters/trustsc-vulkan-winit/Cargo.toml", "Cargo.lock"]
runtime_deployment = true
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Confined to the trustsc-vulkan-winit edge adapter (ADR-012)."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
]

[[entries]]
component_id = "raw-window-handle-0.6.2"
name = "raw-window-handle"
version = "0.6.2"
classification = "SOUP"
supplier = "crates.io / rust-windowing maintainers"
repository = "https://github.com/rust-windowing/raw-window-handle"
license = "MIT OR Apache-2.0"
usage = "Shared windowing-handle trait used to bridge winit windows into ash-window surface creation, confined to the trustsc-vulkan-winit presentation adapter (ADR-012). Compiled into (but never exercised by) tools/trustsc-medui-studio too, for the same reason as ash-window above."
integration_path = "adapters/trustsc-vulkan-winit"
pinned_by = ["adapters/trustsc-vulkan-winit/Cargo.toml", "Cargo.lock"]
runtime_deployment = true
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Confined to the trustsc-vulkan-winit edge adapter (ADR-012). Handle types never cross into governed crates."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
]

[[entries]]
component_id = "winit-0.29.15"
name = "winit"
version = "0.29.15"
classification = "SOUP"
supplier = "crates.io / rust-windowing maintainers"
repository = "https://github.com/rust-windowing/winit"
license = "MIT OR Apache-2.0"
usage = "Cross-platform window creation and event-loop pump, confined to the trustsc-vulkan-winit presentation adapter's App runner (ADR-012). Compiled into (but never exercised by) tools/trustsc-medui-studio too, since it depends on the same adapter crate for its offscreen-only render bridge (ADR-022 wave S7), which calls OffscreenRenderer directly and never constructs an App/event loop."
integration_path = "adapters/trustsc-vulkan-winit"
pinned_by = ["adapters/trustsc-vulkan-winit/Cargo.toml", "Cargo.lock"]
runtime_deployment = true
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Confined to the trustsc-vulkan-winit edge adapter (ADR-012). No winit type crosses into a governed crate's public API."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
    "App::run_from_env keeps CLI/CI entry points (--headless-smoke) independent of window-system availability.",
]

[[entries]]
component_id = "shaderc-0.10.1"
name = "shaderc"
version = "0.10.1"
classification = "SOUP"
supplier = "crates.io / google / shaderc-rs maintainers"
repository = "https://github.com/google/shaderc-rs"
license = "Apache-2.0"
usage = "Compiles GLSL text-shader sources to SPIR-V, confined to the host-only tools/trustsc-shader-baker (ADR-012) that bakes committed .spv artifacts under adapters/trustsc-vulkan-winit/shaders/generated/ as reviewed generated evidence."
integration_path = "tools/trustsc-shader-baker"
pinned_by = ["tools/trustsc-shader-baker/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate wrapping the Google shaderc C++ library; no medical-device qualification or vendor support agreement."
boundary_rationale = "A build-only dependency of tools/trustsc-shader-baker (ADR-007/ADR-012); no application or the trustsc-vulkan-winit adapter depends on shaderc directly — both consume only the committed .spv + report.json evidence."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
    "The shaderc version and compile options are recorded in report.json so verify can detect toolchain drift.",
    "CI only ever runs tools/trustsc-shader-baker verify against committed .spv artifacts, never a rebake.",
]

[[entries]]
component_id = "axum-0.8.9"
name = "axum"
version = "0.8.9"
classification = "SOUP"
supplier = "crates.io / tokio-rs maintainers"
repository = "https://github.com/tokio-rs/axum"
license = "MIT"
usage = "HTTP routing, request extraction, and response building for the hosted MedUI Studio server's healthz/API/static-frontend endpoints (ADR-022)."
integration_path = "tools/trustsc-medui-studio"
pinned_by = ["tools/trustsc-medui-studio/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Confined to the host-only tools/trustsc-medui-studio server (ADR-005 tools zone); never linked into any crates/ or adapters/ code that ships to a device."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
    "The server is documented (crate README) as requiring a reverse proxy for TLS/stronger auth — it is not meant to face an untrusted network directly.",
]

[[entries]]
component_id = "hyper-1.10.1"
name = "hyper"
version = "1.10.1"
classification = "SOUP"
supplier = "crates.io / hyperium maintainers"
repository = "https://github.com/hyperium/hyper"
license = "MIT"
usage = "HTTP/1.x wire protocol implementation underlying axum's server (transitive via axum, not a direct Cargo.toml dependency) for the hosted MedUI Studio server (ADR-022). Registered explicitly, not just implied by axum's entry, since it is the actual HTTP stack terminating client connections."
integration_path = "tools/trustsc-medui-studio (transitive via axum)"
pinned_by = ["Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Confined to the host-only tools/trustsc-medui-studio server (ADR-005 tools zone); never linked into any crates/ or adapters/ code that ships to a device."
risk_controls = [
    "Version tracked via Cargo.lock and reviewed alongside axum's own version bumps (axum pins its compatible hyper range).",
    "The server is documented (crate README) as requiring a reverse proxy for TLS/stronger auth — it is not meant to face an untrusted network directly.",
]

[[entries]]
component_id = "tokio-1.52.3"
name = "tokio"
version = "1.52.3"
classification = "SOUP"
supplier = "crates.io / tokio-rs maintainers"
repository = "https://github.com/tokio-rs/tokio"
license = "MIT"
usage = "Async runtime (multi-thread executor, TCP listener, ctrl-c signal handling) underlying the hosted MedUI Studio server (ADR-022)."
integration_path = "tools/trustsc-medui-studio"
pinned_by = ["tools/trustsc-medui-studio/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Confined to the host-only tools/trustsc-medui-studio server (ADR-005 tools zone); no device-facing crate depends on an async runtime."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
    "Only the feature flags the server actually uses (rt-multi-thread, macros, net, signal) are enabled.",
]

[[entries]]
component_id = "clap-4.6.1"
name = "clap"
version = "4.6.1"
classification = "SOUP"
supplier = "crates.io / clap-rs maintainers"
repository = "https://github.com/clap-rs/clap"
license = "MIT OR Apache-2.0"
usage = "Parses the `--repo`/`--listen` CLI arguments for the hosted MedUI Studio server binary (ADR-022)."
integration_path = "tools/trustsc-medui-studio"
pinned_by = ["tools/trustsc-medui-studio/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Confined to the host-only tools/trustsc-medui-studio binary's argument parsing; no device-facing code depends on it."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
]

[[entries]]
component_id = "include_dir-0.7.4"
name = "include_dir"
version = "0.7.4"
classification = "SOUP"
supplier = "crates.io / Michael-F-Bryan"
repository = "https://github.com/Michael-F-Bryan/include_dir"
license = "MIT OR Apache-2.0"
usage = "Embeds the built frontend/dist/ directory into the MedUI Studio server binary at compile time, so the server has no runtime file dependency (ADR-022)."
integration_path = "tools/trustsc-medui-studio"
pinned_by = ["tools/trustsc-medui-studio/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Confined to the host-only tools/trustsc-medui-studio server; the embedded bytes are frontend assets, not anything a governed crate or adapter consumes."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
]

[[entries]]
component_id = "png-0.18.1"
name = "png"
version = "0.18.1"
classification = "SOUP"
supplier = "crates.io / image-rs maintainers"
repository = "https://github.com/image-rs/image-png"
license = "MIT OR Apache-2.0"
usage = "Encodes captured offscreen frames (RGBA8 from adapters/trustsc-vulkan-winit's OffscreenRenderer::read_pixels) as PNG bytes for the MedUI Studio server's render bridge (ADR-022 wave S7), and decodes them back in the crate's own round-trip test."
integration_path = "tools/trustsc-medui-studio"
pinned_by = ["tools/trustsc-medui-studio/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Confined to the host-only tools/trustsc-medui-studio server (ADR-005 tools zone); the pixels it encodes already exist as plain RGBA8 bytes from the adapter's offscreen render path — no governed crate or adapter code depends on PNG encoding/decoding itself."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
]

[[entries]]
component_id = "tower-0.5.3"
name = "tower"
version = "0.5.3"
classification = "SOUP"
supplier = "crates.io / tower-rs maintainers"
repository = "https://github.com/tower-rs/tower"
license = "MIT"
usage = "Dev-dependency only: `ServiceExt::oneshot` drives the MedUI Studio server's axum Router directly in-process for integration tests (ADR-022), with no network socket involved."
integration_path = "tools/trustsc-medui-studio (dev-dependency)"
pinned_by = ["tools/trustsc-medui-studio/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Test-only dependency of the host-only tools/trustsc-medui-studio crate; never compiled into the release binary's dependency graph beyond what axum itself already pulls in transitively."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
]
