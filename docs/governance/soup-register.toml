schema_version = 1
register_kind = "mdux-soup-register"
scope = "host-tooling-and-presentation-adapters"
owner = "MduX-rust text authoring maintainers"
yocto_policy = "Future Yocto integration consumes approved font assets, notices, manifest metadata, and generated package outputs; it does not install host baker/shader-baker tooling or their third-party crates into the target image. Presentation-adapter dependencies (ash, ash-window, raw-window-handle, winit) are Class B host-bring-up dependencies reviewed per ADR-012; see each entry's runtime_deployment and integration_path fields."

[[entries]]
component_id = "fontdue-0.9.3"
name = "fontdue"
version = "0.9.3"
classification = "SOUP"
supplier = "crates.io / fontdue maintainers"
repository = "https://github.com/mooman219/fontdue"
license = "MIT OR Apache-2.0 OR Zlib"
usage = "Loads vendored TTF bytes, computes glyph metrics, and rasterizes glyph bitmaps during host-only Roboto atlas compilation."
integration_path = "tools/mdux-font-baker"
pinned_by = ["tools/mdux-font-baker/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Used only while baking approved text packages on engineering hosts. Device/runtime artifacts consume only reviewed generated package bytes and digests."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
    "Validate the vendored Roboto digest and byte length against assets/fonts/roboto/font-manifest.toml before rasterization.",
    "Require deterministic rebuild verification against generated/fonts/roboto-regular-16px outputs.",
]

[[entries]]
component_id = "serde-1.0.228"
name = "serde"
version = "1.0.228"
classification = "SOUP"
supplier = "crates.io / serde-rs maintainers"
repository = "https://github.com/serde-rs/serde"
license = "MIT OR Apache-2.0"
usage = "Deserializes host-only bake recipes and generated package.json inputs, and serializes reviewed bake outputs. Also deserializes authored scenario TOML (ADR-016 §4) in crates/mdux-build, which compiles them to static Rust data consumed at runtime. Also deserializes ML bake recipes and evidence emission in tools/mdux-ml-baker (ADR-017), and shader-bake recipes/reports in tools/mdux-shader-baker (ADR-012)."
integration_path = "tools/mdux-font-baker, crates/mdux/build.rs, crates/mdux-build (scenario compilation), tools/mdux-ml-baker, tools/mdux-shader-baker"
pinned_by = ["tools/mdux-font-baker/Cargo.toml", "crates/mdux/Cargo.toml", "crates/mdux-build/Cargo.toml", "tools/mdux-ml-baker/Cargo.toml", "tools/mdux-shader-baker/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Confined to host authoring input/output handling; no serde API crosses into the runtime package contract."
risk_controls = [
    "Use deny_unknown_fields on bake-side schemas to prevent silent config drift.",
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
]

[[entries]]
component_id = "serde-json-1.0.150"
name = "serde_json"
version = "1.0.150"
classification = "SOUP"
supplier = "crates.io / serde-rs maintainers"
repository = "https://github.com/serde-rs/json"
license = "MIT OR Apache-2.0"
usage = "Emits deterministic host-side package.json and report.json artifacts for review and reproducibility checks, and parses approved package.json inputs into generated Rust source during mdux builds. Also parses the safetensors container header in crates/mdux-ml-authoring (ADR-017 §6, avoiding a dedicated safetensors crate dependency), and parses/emits ML model package.json/report.json evidence in tools/mdux-ml-baker and crates/mdux-build model codegen, and shader package.json/report.json evidence in tools/mdux-shader-baker (ADR-012)."
integration_path = "tools/mdux-font-baker, crates/mdux/build.rs, crates/mdux-ml-authoring, tools/mdux-ml-baker, crates/mdux-build (model codegen), tools/mdux-shader-baker"
pinned_by = ["tools/mdux-font-baker/Cargo.toml", "crates/mdux/Cargo.toml", "crates/mdux-ml-authoring/Cargo.toml", "tools/mdux-ml-baker/Cargo.toml", "crates/mdux-build/Cargo.toml", "tools/mdux-shader-baker/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Only the generated JSON artifacts cross the boundary; the crate itself remains a host build dependency."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
    "Use byte-for-byte verification to confirm generated JSON artifacts match a fresh rebuild.",
]

[[entries]]
component_id = "sha2-0.10.9"
name = "sha2"
version = "0.10.9"
classification = "SOUP"
supplier = "crates.io / RustCrypto maintainers"
repository = "https://github.com/RustCrypto/hashes"
license = "MIT OR Apache-2.0"
usage = "Computes manifest, atlas, package, and report digests during deterministic host-side baking and verification. Also computes weights/package/report digests in crates/mdux-ml-authoring and tools/mdux-ml-baker (ADR-017), and shader package/report digests in tools/mdux-shader-baker (ADR-012)."
integration_path = "tools/mdux-font-baker, crates/mdux-text-authoring, crates/mdux-ml-authoring, tools/mdux-ml-baker, tools/mdux-shader-baker"
pinned_by = ["tools/mdux-font-baker/Cargo.toml", "crates/mdux-text-authoring/Cargo.toml", "crates/mdux-ml-authoring/Cargo.toml", "tools/mdux-ml-baker/Cargo.toml", "tools/mdux-shader-baker/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Digest values are consumed as evidence, but the hashing crate stays in host-side tooling and authoring flows."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
    "Record resulting SHA-256 values in source manifests and generated reports for independent verification.",
]

[[entries]]
component_id = "toml-1.1.2+spec-1.1.0"
name = "toml"
version = "1.1.2+spec-1.1.0"
classification = "SOUP"
supplier = "crates.io / toml-rs maintainers"
repository = "https://github.com/toml-rs/toml"
license = "MIT OR Apache-2.0"
usage = "Parses font manifests and bake recipes that define approved Roboto source inputs and Yocto handoff metadata. Also parses authored scenario scripts (examples/<app>/verify/scenarios/*.toml, ADR-016 §4) in crates/mdux-build, compiling them into static ScenarioScript data at build time — no TOML parsing at runtime. Also parses host-only ML bake recipes in tools/mdux-ml-baker (ADR-017) and shader-bake recipes in tools/mdux-shader-baker (ADR-012)."
integration_path = "tools/mdux-font-baker, crates/mdux-build (scenario compilation), tools/mdux-ml-baker, tools/mdux-shader-baker"
pinned_by = ["tools/mdux-font-baker/Cargo.toml", "crates/mdux-build/Cargo.toml", "tools/mdux-ml-baker/Cargo.toml", "tools/mdux-shader-baker/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "TOML parsing is confined to host-side authoring configuration (font manifests/recipes, and now scenario scripts); target systems consume the already-reviewed generated outputs instead."
risk_controls = [
    "Fail parsing on invalid manifests or recipes rather than recovering silently.",
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
]

[[entries]]
component_id = "ash-0.38.0+1.3.281"
name = "ash"
version = "0.38.0+1.3.281"
classification = "SOUP"
supplier = "crates.io / ash maintainers"
repository = "https://github.com/ash-rs/ash"
license = "MIT OR Apache-2.0"
usage = "Vulkan 1.x bindings for instance/device/swapchain setup, pipeline creation, and frame submission, confined to the mdux-vulkan-winit presentation adapter (ADR-012) and used transitively by every example that depends on it (hello_world, class_c_monitor)."
integration_path = "adapters/mdux-vulkan-winit"
pinned_by = ["adapters/mdux-vulkan-winit/Cargo.toml", "Cargo.lock"]
runtime_deployment = true
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Confined to the mdux-vulkan-winit edge adapter (ADR-012). No ash::vk type crosses into a governed crate's public API."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
    "The adapter only accepts owned mdux types (Framework, CompiledScreenPackage, ScreenTextLayout) at its public boundary.",
]

[[entries]]
component_id = "ash-window-0.13.0"
name = "ash-window"
version = "0.13.0"
classification = "SOUP"
supplier = "crates.io / ash maintainers"
repository = "https://github.com/ash-rs/ash"
license = "MIT OR Apache-2.0"
usage = "Creates a Vulkan surface from a winit window handle, confined to the mdux-vulkan-winit presentation adapter (ADR-012)."
integration_path = "adapters/mdux-vulkan-winit"
pinned_by = ["adapters/mdux-vulkan-winit/Cargo.toml", "Cargo.lock"]
runtime_deployment = true
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Confined to the mdux-vulkan-winit edge adapter (ADR-012)."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
]

[[entries]]
component_id = "raw-window-handle-0.6.2"
name = "raw-window-handle"
version = "0.6.2"
classification = "SOUP"
supplier = "crates.io / rust-windowing maintainers"
repository = "https://github.com/rust-windowing/raw-window-handle"
license = "MIT OR Apache-2.0"
usage = "Shared windowing-handle trait used to bridge winit windows into ash-window surface creation, confined to the mdux-vulkan-winit presentation adapter (ADR-012)."
integration_path = "adapters/mdux-vulkan-winit"
pinned_by = ["adapters/mdux-vulkan-winit/Cargo.toml", "Cargo.lock"]
runtime_deployment = true
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Confined to the mdux-vulkan-winit edge adapter (ADR-012). Handle types never cross into governed crates."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
]

[[entries]]
component_id = "winit-0.29.15"
name = "winit"
version = "0.29.15"
classification = "SOUP"
supplier = "crates.io / rust-windowing maintainers"
repository = "https://github.com/rust-windowing/winit"
license = "MIT OR Apache-2.0"
usage = "Cross-platform window creation and event-loop pump, confined to the mdux-vulkan-winit presentation adapter's App runner (ADR-012)."
integration_path = "adapters/mdux-vulkan-winit"
pinned_by = ["adapters/mdux-vulkan-winit/Cargo.toml", "Cargo.lock"]
runtime_deployment = true
yocto_target_dependency = false
support_model = "Community-maintained crate; no medical-device qualification or vendor support agreement."
boundary_rationale = "Confined to the mdux-vulkan-winit edge adapter (ADR-012). No winit type crosses into a governed crate's public API."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
    "App::run_from_env keeps CLI/CI entry points (--headless-smoke) independent of window-system availability.",
]

[[entries]]
component_id = "shaderc-0.10.1"
name = "shaderc"
version = "0.10.1"
classification = "SOUP"
supplier = "crates.io / google / shaderc-rs maintainers"
repository = "https://github.com/google/shaderc-rs"
license = "Apache-2.0"
usage = "Compiles GLSL text-shader sources to SPIR-V, confined to the host-only tools/mdux-shader-baker (ADR-012) that bakes committed .spv artifacts under adapters/mdux-vulkan-winit/shaders/generated/ as reviewed generated evidence."
integration_path = "tools/mdux-shader-baker"
pinned_by = ["tools/mdux-shader-baker/Cargo.toml", "Cargo.lock"]
runtime_deployment = false
yocto_target_dependency = false
support_model = "Community-maintained crate wrapping the Google shaderc C++ library; no medical-device qualification or vendor support agreement."
boundary_rationale = "A build-only dependency of tools/mdux-shader-baker (ADR-007/ADR-012); no application or the mdux-vulkan-winit adapter depends on shaderc directly — both consume only the committed .spv + report.json evidence."
risk_controls = [
    "Keep the crate version pinned in Cargo.lock and review updates through normal dependency governance.",
    "The shaderc version and compile options are recorded in report.json so verify can detect toolchain drift.",
    "CI only ever runs tools/mdux-shader-baker verify against committed .spv artifacts, never a rebake.",
]
